name: One-time user update integrity fix

on:
  push:
    branches: [desktop-v1]
    paths:
      - 'scripts/patch-user-update-integrity.mjs'
      - '.github/workflows/one-time-user-update-integrity.yml'

permissions:
  contents: write

jobs:
  patch-context:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout desktop branch
        uses: actions/checkout@v4
        with:
          ref: desktop-v1
          fetch-depth: 0

      - name: Apply guarded user integrity patch
        run: node scripts/patch-user-update-integrity.mjs

      - name: Commit patch if needed
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No user update changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/context/AppContext.tsx
          git commit -m "Fix user update uniqueness and employee synchronization"
          git push origin HEAD:desktop-v1
